pour cloner un repository il faut faire "git clone + la clé du repository"

git add .

git commit -m "message"

git push

git pull

pour creer une branche on fait git branch +nom de la branche

et pour switcher on fait git checkout +nom de la branch
